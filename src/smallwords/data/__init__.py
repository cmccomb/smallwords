"""Bundled text resources for built-in source-backed word lists."""

# This subpackage exists so setuptools can ship the normalized text resources.
# The package stays otherwise empty because the text files are loaded via importlib.resources.
